# 复制为 .env.local 后按需填写。这些变量都只在服务端读取（切勿加 NEXT_PUBLIC_ 前缀）。
# 全部可选：留空时使用代码中的默认值，详见 README。

GITHUB_OWNER=liwenka1
GITHUB_REPO=speak-mind
DIARY_LABEL=diary

# 只读 PAT。本地不填也能跑；部署到 Vercel 时强烈建议配置，避免共享出口 IP 被限流。
GITHUB_TOKEN=
